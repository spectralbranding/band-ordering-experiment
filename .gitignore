.venv/
__pycache__/
*.pyc

# macOS cruft — do not rely on a maintainer's global gitignore
.DS_Store

# secrets — never commit
.env
.env.*
*.key
*.pem
secrets.yaml

# fetched on first run, not part of the artifact
output/nwn_pad131m.txt
output/logs/*.log
